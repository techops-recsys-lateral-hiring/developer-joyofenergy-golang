# top-most EditorConfig file
root = true

# Unix-style for every file
[*]
charset = utf-8

end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true

indent_style = space
indent_size = 2

# Unix-style for only Makefile
[Makefile]
indent_style = tab
